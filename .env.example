# API base URL (backend)
VITE_API_URL=http://localhost:8000

# Stripe publishable key (test mode — safe for frontend, but keep real key in .env)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here